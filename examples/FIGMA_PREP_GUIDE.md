# Figma Prep Guide (MVP)

1. Name page-level frames: `page:home`, `page:about`
2. Use text style names: H1, H2, H3, Body
3. Group logical sections with frames named `section-*`
4. Reusable component prefix: `comp-`
5. Keep to Auto Layout where possible (avoid overlapping absolute layers).